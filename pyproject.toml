[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "audiobook-tts-benchmark"
version = "0.1.0"
description = "CLI for comparing Russian audiobook text-to-speech models"
readme = "Readme.md"
requires-python = ">=3.11"
dependencies = [
    "elevenlabs>=2.0",
    "google-genai>=2.23",
    "python-dotenv>=1.0",
    "requests>=2.32",
]

[project.scripts]
audiobook-tts = "audiobook_tts.cli:main"
audiobook-tts-corpus = "audiobook_tts.corpus_cli:main"

[tool.setuptools.packages.find]
include = ["audiobook_tts*"]
